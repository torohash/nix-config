# OpenWhispr入力不具合の調査報告で使う指示対象の対応表

この表の1行は、OpenWhisprの入力経路にある一つの具体対象と、その対象が調査報告で担う一つの役割を表す。

| 出典 | 目的 | 具体対象 | 役割 | 前後関係 | 候補語 | 初出定義 |
|---|---|---|---|---|---|---|
| `nix/home/common/openwhispr.nix:9-17`、現在のGNOME設定とD-Bus登録 | ショートカットからOpenWhisprへ要求が届く構成を特定する | `Ctrl+Shift+K`がセッションD-Busの`com.openwhispr.App.Toggle`を呼び、実行中のOpenWhisprがそのメソッドを公開している構成 | 手段 | ユーザー操作の直後、録音処理の前 | 起動経路 | 確立用語 |
| 現在のOpenWhisprプロセス、PipeWire、マイクデバイスの状態 | 録音と文字起こしを実行できる前提、および未実施の確認を区別する | OpenWhispr 1.8.1が実行中で、PipeWireとマイクが利用可能だが、実際の録音・文字起こしは試していない状態 | 状態 | 起動経路の後、自動入力の前 | 音声認識の状態 | 確立用語 |
| `nix/home/common/ydotool.nix:3-17`、`docs/packages.md:52,57` | 文字起こし結果を対象アプリへ送る構成を特定する | Wayland上で`ydotoold`が`/dev/uinput`を使い、文字起こし結果を対象アプリへ仮想キーボード入力する構成 | 手段 | 音声認識の後、対象アプリへの到達前 | 自動入力経路 | 確立用語 |
| 現在の`ydotoold.service`状態とjournal | 自動入力経路が途切れている位置を確定する | `ydotoold`が`/dev/uinput`を開いた際に`Permission denied`となり、終了状態2で停止している状態 | 状態 | 自動入力経路の開始時、対象アプリへの到達前 | 確定した故障点 | 「確定した故障点」とは、現在の証拠だけで入力不能との因果関係を確定できる位置を指す |
| 現在の`/dev/uinput`モード、所有者、ユーザーのグループ、udevルール | `ydotoold`停止の直接原因と修正対象を特定する | `/dev/uinput`が`root:root`所有の`0600`で、実行ユーザーに書込権限がなく、権限を付与するudevルールもない状態 | 状態 | `ydotoold`の起動時、権限拒否の直前 | `/dev/uinput`の権限不足 | 確立用語 |
| OpenWhisprの実操作を行わない調査条件 | 確定した故障点と、追加確認が必要な範囲を分ける | ショートカットの実押下、録音、文字起こし、プロバイダー設定、APIキー、対象アプリへの入力結果を確認していない状態 | 状態 | 現在の調査完了後、必要な実操作確認の前 | 未確認範囲 | 確立用語 |
